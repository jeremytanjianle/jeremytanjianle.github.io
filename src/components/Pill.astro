<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
		padding: 0.35rem 0;
		gap: 0.5rem;
		color: var(--gray-300);
		border-bottom: 1px solid var(--gray-700);
		font-size: var(--text-sm);
		line-height: 1.35;
		white-space: nowrap;
	}
</style>
